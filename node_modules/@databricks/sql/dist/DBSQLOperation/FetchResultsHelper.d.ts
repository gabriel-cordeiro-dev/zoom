import { TFetchResultsResp, TOperationHandle, TRowSet } from '../../thrift/TCLIService_types';
import HiveDriver from '../hive/HiveDriver';
export default class FetchResultsHelper {
    private readonly driver;
    private readonly operationHandle;
    private fetchOrientation;
    private prefetchedResults;
    private readonly returnOnlyPrefetchedResults;
    hasMoreRows: boolean;
    constructor(driver: HiveDriver, operationHandle: TOperationHandle, prefetchedResults: Array<TFetchResultsResp | undefined>, returnOnlyPrefetchedResults: boolean);
    private processFetchResponse;
    fetch(maxRows: number): Promise<TRowSet | undefined>;
}
